import React from "react";

// Material UI
import Button from "@material-ui/core/Button";

function makeIndex(){

}

function FaunaTest( props ){

    return(
        <Button onClick={makeIndex}>
            Create Index
        </Button>
    )

}

export default FaunaTest;